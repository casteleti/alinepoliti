# Sistema operacional
.DS_Store
Thumbs.db

# Logs
*.log

# Configuração local com segredos REAIS (não versionar) —
# em produção, edite app/config.php no servidor ou use variáveis de ambiente.
/app/config.local.php
.env

# Dependências (não há no momento; por garantia)
/vendor/
/node_modules/

# Uploads de mídia gerados em produção (se houver no futuro)
/public/uploads/

# Imagens originais (pesadas) — mantidas no disco, fora do repo
/public/assets/_originais/

# Node (build de CSS) — node_modules não é versionado; o app.css compilado sim
node_modules/
